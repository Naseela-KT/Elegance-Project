<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            padding-top: 50px;
            padding-bottom: 50px;
        }

        .invoice-container {
            /* background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);*/
            margin: 0 auto; 
            max-width: 650px;
          
        }

        .company-logo {
            max-width: 150px;
            margin-bottom: 20px;
        }

        .address {
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th,
        td {
            border: 1px solid #dee2e6;
            padding: 12px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        .total {
            margin-top: 20px;
            text-align: right;
        }

        .footer {
            margin-top: 30px;
            text-align: center;
            color: #777;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="invoice-container">
            <div class="row" style="margin-bottom: 50px;">
                <div class="col-md-6">
                    <img src="http://www.elegancestores.online/assets/imgs/theme/elegance.svg" alt="Company Logo" class="company-logo" height="60" width="100">
                </div>
                <div class="col-md-6 text-end">
                    <h6 style="font-weight: 600;">Tax Invoice/Bill of Supply/Cash Memo
                        (Original for Recipient)</h6>
                </div>
            </div>

            <div class="row address">
                <div class="col-md-6">
                    <h6><strong>Shipping Address:</strong></h6>
                    <p class="mb-1 font-sm">
                        <%=address.name%>,<br><%=address.housename%> House,<br><%=address.area%>,<%=address.city%>,<br> <%=address.state%>,<br><%=address.pincode%>
                    </p>
                </div>
                <div class="col-md-6">
                    <h6 class="text-end"><strong>Billing Address:</strong></h6>
                    <p class="mb-1 font-sm text-end ">
                        <%=address.name%>,<br><%=address.housename%> House,<br><%=address.area%>,<%=address.city%>,<br> <%=address.state%>,<br><%=address.pincode%>
                    </p>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6">
                    <% const originalDate = new Date(`${order.createdOn}`); %>
                    <% const formattedDate = originalDate.toLocaleDateString('en-IN'); %>
                    <p><strong>Order ID:</strong> #<%=order.orderId%></p>
                    <p><strong>Order Date:</strong> <%=formattedDate%></p>
                </div>
            </div>

            <table class="table">
                <thead>
                    <tr>
                        <th>Sl.No</th>
                        <th>Product Name</th>
                        <th>Unit Price</th>
                        <th>Quantity</th>
                        <th>Net Amount</th>
                        
                    </tr>
                </thead>
                <tbody>
                    <!-- Add product details dynamically based on your order -->
                    <%for(let i=0;i<items.length;i++){%>
                        <tr >
                            <td ><%=i+1%></td>
                            <td ><%=product[i]%></td>
                            <td >₹<%=items[i].subtotal/items[i].quantity%></td>
                            <td ><%=items[i].quantity%></td>
                            <td >₹<%=items[i].subtotal%></td>
                          
                        </tr>
                    <%}%>
                    <tr>
                        <td colspan="4"><strong>TOTAL:</strong></td>
                        <td>₹<%=order.totalAmount%></td>
                    </tr>
                </tbody>
            </table>

            

            
        </div>
    </div>
    <footer class="footer"><p>Thank you for your purchase!</p></footer>

    <!-- Bootstrap JS (optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>